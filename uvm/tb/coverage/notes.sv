


Functional coverage tells you what scenarios you exercised — it's a user-defined model of "interesting things that must happen before I trust this design."
 Your covergroup says: I must see reads on both sides, both request and response phases, all address regions, all prot values, and specific response codes. 
 The simulator counts hits; closure means every bin got hit at least once (by default, option.at_least = 1).

